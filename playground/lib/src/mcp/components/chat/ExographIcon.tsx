import { default as LogoNoText } from "../../../../public/logo-no-text.svg";

export function ExographIcon({ className }: { className?: string }) {
  return <LogoNoText className={className} alt="Exograph" />;
}
