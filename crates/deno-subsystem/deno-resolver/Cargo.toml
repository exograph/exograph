[package]
name = "deno-resolver"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow.workspace = true
async-graphql-parser.workspace = true
async-graphql-value.workspace = true
async-recursion.workspace = true
async-trait.workspace = true
# Make sure deno_core version matches the one in the payas_deno's Cargo.toml
# If we re-export the deno_core crate from the payas_deno crate (instead of declaring a dependency here), 
# the #[op] macro panics (it looks for the deno_core crate in crate's local dependencies)
deno_core.workspace = true
futures.workspace = true
serde_json = { workspace = true, features = ["preserve_order"] }
maybe-owned.workspace = true
thiserror.workspace = true
tokio.workspace = true

payas-deno = { path = "../../../libs/payas-deno" }
core-plugin-interface = { path = "../../core-subsystem/core-plugin-interface" }
deno-model = { path = "../deno-model" }


[dev-dependencies]
tokio.workspace = true
builder = { path = "../../builder" }


[lib]
crate-type = ["lib"]
