[package]
name = "postgres-model-builder-dynamic"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
core-plugin-interface = { path = "../../core-subsystem/core-plugin-interface" }
postgres-model-builder = { path = "../postgres-model-builder" }

[dev-dependencies]

[lib]
crate-type = ["cdylib"]
