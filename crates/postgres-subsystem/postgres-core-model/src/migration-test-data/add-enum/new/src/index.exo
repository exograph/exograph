@postgres
module TodoModule {
    type Todo {
        @pk id: Int = autoIncrement()
        title: String
        priority    : Priority
    }

    enum Priority {
        LOW
        MEDIUM
        HIGH
    }
}
