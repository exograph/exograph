[package]
name = "postgres-rest-resolver"
version.workspace = true
edition.workspace = true
publish = false

[dependencies]
async-trait.workspace = true
http.workspace = true

exo-sql-pg = { path = "../../../libs/exo-sql/pg" }
exo-sql-pg-connect = { path = "../../../libs/exo-sql/pg-connect" }

core-resolver = { path = "../../core-subsystem/core-resolver" }
postgres-rest-model = { path = "../postgres-rest-model" }
postgres-core-resolver = { path = "../postgres-core-resolver" }
common = { path = "../../common" }

[dev-dependencies]

[lib]
doctest = false
