[package]
name = "postgres-builder-dynamic"
version.workspace = true
edition.workspace = true
publish = false

[dependencies]
core-plugin-interface = { path = "../../core-subsystem/core-plugin-interface" }
postgres-builder = { path = "../postgres-builder" }

[lib]
crate-type = ["cdylib"]
doctest = false
test = false
