[package]
name = "core-plugin-shared"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = "1.0"
bincode = "1.3.3"
thiserror = "1.0.30"
bytes = "1"

core-model = { path = "../core-model" }

[dev-dependencies]

[lib]
