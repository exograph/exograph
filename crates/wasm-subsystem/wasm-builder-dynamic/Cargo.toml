[package]
name = "wasm-builder-dynamic"
version.workspace = true
edition.workspace = true
publish = false

[dependencies]
core-plugin-interface = { path = "../../core-subsystem/core-plugin-interface" }
wasm-builder = { path = "../wasm-builder" }

[dev-dependencies]

[lib]
crate-type = ["cdylib"]
doctest = false
