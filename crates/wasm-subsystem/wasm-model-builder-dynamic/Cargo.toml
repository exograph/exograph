[package]
name = "wasm-model-builder-dynamic"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
core-plugin-interface = { path = "../../core-subsystem/core-plugin-interface" }
wasm-model-builder = { path = "../wasm-model-builder" }

[dev-dependencies]

[lib]
crate-type = ["cdylib"]
