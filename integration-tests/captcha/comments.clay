import "captcha.clay"

@postgres
service CommentsPersistence {
    @access(
        query=true,
        mutation=CaptchaContext.isValid
    )
    type Comment {
        id: Int = autoincrement() @pk
        text: String
    }
}