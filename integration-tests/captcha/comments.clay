import "captcha.clay"

@postgres
service CommentsPersistence {
    @access(
        query=true,
        mutation=CaptchaContext.isValid
    )
    type Comment {
        @pk id: Int = autoIncrement()
        text: String
    }
}