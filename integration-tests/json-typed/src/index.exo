@postgres
module TodoModule {
  @access(true)
  type Todo {
    @pk id: Int = autoIncrement()
    title: String
    metadata: Metadata
  }

  @access(true)
  @json
  type Metadata {
    expectedEffortHrs: Int?
    notes: String
    completionDate: LocalDate
    // tags: Array<String>
  }
}
