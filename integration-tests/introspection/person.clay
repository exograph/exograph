model Person {
  id: Int @pk @autoincrement
  name: String
}

@external("logger.js")
service Logger {
  input type LogMessage {
    level: String
    message: String
  }

  export query logger(@inject env: Env, input: LogMessage): Boolean
  mutation debug(@inject env: Env, input: LogMessage): Boolean
}