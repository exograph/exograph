model Person {
  id: Int = autoincrement() @pk
  name: String
}

@external("logger.js")
service Logger {
  input type LogMessage {
    level: String
    message: String
  }

  export query logger(@inject claytip: Claytip, input: LogMessage): Boolean
  mutation debug(@inject claytip: Claytip, input: LogMessage): Boolean
}