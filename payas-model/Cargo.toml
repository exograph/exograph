[package]
name = "payas-model"
version = "0.1.0"
edition = "2021"

[dependencies]
typed-generational-arena = { version = "0.2", features = ["serde"] }
serde = "1.0"
payas-sql = { path = "../payas-sql" }

[lib]
